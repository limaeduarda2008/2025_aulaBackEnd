let nr = 1;
let nrAlunos = 32;

//verifica condição, se for true entra no bloco  - faz a condição e depois entra no bloco
while (nr <= nrAlunos) {
    console.log(`Nº ${nr}? `);
    nr++;  //nr = nr + 1 (pode usar qualquer um dos dois)
}
console.log('Iniciar a aula!');

