const usuario = "duda";
const senha = "0912";

const usuarioDigitado = "duda";
const senhaDigitada = "0912";

if (usuarioDigitado !== usuario) {
    console.log("Usuário inválido");
} else if (senhaDigitada !== senha) {
    console.log("Senha inválida");
} else {
    console.log("Usuário logado");
}
